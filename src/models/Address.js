class Address {
    constructor({ street, city, zipcode, geo }) {
        this.street = street;
        this.city = city;
        this.zipcode = zipcode;
        this.geo = geo;
    }
}

export default Address

